# ============================================
# REQUIRED ENVIRONMENT VARIABLES
# ============================================

# Better Auth (minimum 32 characters)
AUTH_SECRET=your-secret-key-at-least-32-characters-long

# Database
DATABASE_URL=postgresql://dondecargo:dondecargo@localhost:5432/dondecargo

# ============================================
# OPTIONAL ENVIRONMENT VARIABLES
# ============================================

# Redis (optional, recommended for production)
REDIS_URL=redis://localhost:6379

# Email Verification (optional feature)
# Set ENABLE_EMAIL_VERIFICATION=true to enable email verification for new users
# If enabled, users must verify their email before they can sign in
ENABLE_EMAIL_VERIFICATION=true

# Loops.js Email Service (optional, required if email verification is enabled)
# Get your API key from https://loops.so
LOOPS_API_KEY=

# Custom Email Template ID (optional, uses default if not set)
# Create a custom verification email template in Loops.js and use its ID here
LOOPS_EMAIL_VERIFICATION_TEMPLATE_ID=

# Custom Email Template ID (optional, uses default if not set)
# Create a custom report price thank you email template in Loops.js and use its ID here
LOOPS_REPORT_PRICE_TEMPLATE_ID=

# Custom Email Template ID (optional, uses default if not set)
# Create a custom report comment template in Loops.js and use its ID here
LOOPS_REPORT_COMMENT_TEMPLATE_ID=

# Custom Email Template ID (optional, uses default if not set)
# Create a custom station creation thank you email template in Loops.js and use its ID here
# Variables available: {{name}}, {{stationName}}, {{address}}, {{fuelTypes}}
LOOPS_CREATE_STATION_TEMPLATE_ID=

# Custom Email Template ID (optional, uses default if not set)
# Create a custom welcome email template in Loops.js and use its ID here
LOOPS_WELCOME_TEMPLATE_ID=

# Custom Email Template ID (optional, uses default if not set)
# Create a custom contact email template in Loops.js and use its ID here
LOOPS_CONTACT_TEMPLATE_ID=

# Custom Email Template ID (optional, uses default if not set)
# Create a custom news email template in Loops.js and use its ID here
LOOPS_NEWS_TEMPLATE_ID=

# News email destination (optional, required if news feature is enabled)
NEWS_EMAIL_TO=

# Contact email destination (optional, required if contact feature is enabled)
CONTACT_TO_EMAIL=

# Google Maps API (optional, enables automatic validation and auto-complete for station creation)
# Get your API key at https://console.cloud.google.com/
# See docs/google-places-api.md for detailed setup instructions
# Without this key, station creation will work but without automatic validation
GOOGLE_MAPS_API_KEY=

# Google AdSense Publisher ID (optional, required if you enable ads)
# Example format: ca-pub-XXXXXXXXXXXXXXXX
NEXT_PUBLIC_ADSENSE_PUBLISHER_ID=

# Google AdSense Ad Slots (get these from AdSense dashboard)
# Map search page sidebar ad (vertical 160x600)
NEXT_PUBLIC_ADSENSE_SLOT_MAP_SIDEBAR=
# Map search page mobile footer ad (horizontal banner)
NEXT_PUBLIC_ADSENSE_SLOT_MAP_MOBILE=
# Station detail page inline ad (responsive)
NEXT_PUBLIC_ADSENSE_SLOT_STATION_INLINE=

# Custom Email Template ID (optional, uses default if not set)
# Create a custom station approved email template in Loops.js and use its ID here
# Variables available: {{name}}, {{stationName}}, {{address}}, {{stationUrl}}
LOOPS_STATION_APPROVED_TEMPLATE_ID=

# Custom Email Template ID (optional, uses default if not set)
# Create a custom station rejected email template in Loops.js and use its ID here
# Variables available: {{name}}, {{stationName}}, {{address}}, {{reason}}
LOOPS_STATION_REJECTED_TEMPLATE_ID=

# Custom Email Template ID for station resubmited
LOOPS_STATION_RESUBMITTED_TEMPLATE_ID=
